export interface Task {
    taskId: string;
    taskName: string;
    taskDescription: string;
    taskPayment: number;
    taskPaymentType: string;
    taskDocument: number;
    taskDays: number;
    taskFrequency: number;
    taskAction: string;
}
